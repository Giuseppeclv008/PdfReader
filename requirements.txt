pymupdf>=1.23.0
tqdm>=4.66.0
markdown>=3.5

# Optional — required only for format 7 (OCR):
# Also install the Tesseract binary:
#   macOS:  brew install tesseract tesseract-lang
#   Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-ita
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10
pillow>=10.0.0

# Required for format 7 math formula recognition (applied per embedded image, not full page):
pix2tex>=0.1.0

# Optional — required only for format 7 AI OCR cleanup (--ai_clean):
# Needs Ollama daemon running locally + a model pulled (default: qwen2.5:3b).
# Install Ollama: https://ollama.com/download
# Pull model:    ollama pull qwen2.5:3b
ollama>=0.3.0

# Optional — required only for format 7 table extraction on scanned pages.
# img2table 0.0.12 uses pre-1.0 polars APIs (`pl.from_dicts(dicts=...)`,
# `pl.max([...])`); the table_helpers module shims `from_dicts`, but the rest
# needs polars <0.17.
img2table>=0.0.12
polars<0.17

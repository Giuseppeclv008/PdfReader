pymupdf>=1.23.0
tqdm>=4.66.0
markdown>=3.5

# Optional — required only for format 7 (OCR):
# Also install the Tesseract binary:
#   macOS:  brew install tesseract tesseract-lang
#   Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-ita
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki
pytesseract>=0.3.10
pillow>=10.0.0

# Required for format 7 math formula recognition (applied per embedded image, not full page):
pix2tex>=0.1.0

# Optional — required only for format 7 AI OCR cleanup (--ai_clean):
# Needs Ollama daemon running locally + a model pulled (default: gemma3:4b).
# Install Ollama: https://ollama.com/download
# Pull model:    ollama pull gemma3:4b
ollama>=0.3.0
